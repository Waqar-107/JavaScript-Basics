# JavaScript-Basics

## placement

1. if we want to have a script run on some event e.g: onClick(), then place the script in the ehad section of the html file.
2. if we need the script to run as the page loads so that the script generates content in the page, then the script should be placed in the body section.
